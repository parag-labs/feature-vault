{
  "name": "feature-vault",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A minimal feature store with point-in-time-correct (as-of) joins (TypeScript port).",
  "scripts": {
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
